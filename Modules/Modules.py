#Basics
import Converters #Import the file Converters.py where we defined functions
from Converters import kg_to_lbs #Calls the specific function from the Converters.py file

kg_to_lbs(100) #Runs the argument weight value of 100 through the specified function

print(Converters.kg_to_lbs(70)) #Print the output of argument 70 if it were passed through the Converters.py file